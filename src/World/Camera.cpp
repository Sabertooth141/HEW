//
// Created by saber on 1/15/2026.
//

#include "Camera.h"

Camera::Camera(const int inX, const int inY, const int inWidth, const int inHeight)
{
    x = inX;
    y = inY;
    viewW = inWidth;
    viewH = inHeight;
}
